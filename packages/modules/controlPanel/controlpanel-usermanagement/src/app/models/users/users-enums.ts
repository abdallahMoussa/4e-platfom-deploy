export enum UserTypeEnum {
  systemUser,
  domainUser,
}
export enum UserGender {
  Male,
  Female,
}

export enum UserPosition {
  Manager,
  Employee,
}

export enum JobTime {
  Main,
  Delegated,
}
export enum UnifiedEntry {
  Enabled,
  Disabled,
}
export enum EnablePermission {
  Enabled,
  Disabled,
}
